spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test2
    username: root
    password: 12345678
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    generate-ddl: true

server:
  port: 9000

